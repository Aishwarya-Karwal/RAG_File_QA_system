from loaders import app

if __name__ == "__main__":
    # This will execute the code inside app.py
    # because Python will treat it as part of the package
    app.main()   # (we’ll add a `main()` function in app.py)
